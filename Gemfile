source 'https://rubygems.org'

ruby '2.3.1'

gem 'rake'
gem 'sinatra'
gem 'require_all'
gem 'slim'

gem 'sprockets'
gem 'sprockets-sass'
gem 'sprockets-helpers'
gem 'sass'

group :development do
  gem 'shotgun'
end

group :test do
end

group :production do
  gem 'thin'
end
